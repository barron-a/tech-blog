<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta comment-header mt-2">
            Comment by {{user.username}}
        </div>
        <div class="text comment-body">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>